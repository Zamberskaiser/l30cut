/**
 * Drag-and-drop contract between the media bin and the timeline lanes.
 * A custom MIME type keeps foreign drags (files, text) from being interpreted
 * as asset drops.
 */
export const ASSET_DND_MIME = "application/x-l30cut-asset";
